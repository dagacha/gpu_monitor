# Core TUI
textual>=0.88.0

# Windows GPU counter access
pywin32>=308; sys_platform == "win32"

# NVIDIA NVML bindings (GPU metrics without spawning nvidia-smi)
nvidia-ml-py>=12.560.30

# System metrics (CPU, RAM)
psutil>=6.0.0

# LibreHardwareMonitor DLL access (used for GPU/CPU temps, clocks, power)
pythonnet>=3.0.5; sys_platform == "win32"
