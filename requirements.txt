# Core TUI
textual>=0.88.0

# Windows GPU counter access
pywin32>=307

# System metrics (CPU, RAM)
psutil>=6.0.0

# LibreHardwareMonitor WMI bridge (for GPU temp, clock, power)
wmi>=1.5.1

# LHM DLL access (used for GPU/CPU temps, clocks, power)
pythonnet>=3.0.3

# NVIDIA on Windows: NVML bindings (preferred over nvidia-smi when present).
# Optional — the collector falls back to nvidia-smi subprocess if missing.
# (nvidia-ml-py replaces the deprecated pynvml package, same `import pynvml`.)
nvidia-ml-py>=12.535.0
